{% extends 'base.html.twig' %}
  
{% block title %}New Task{% endblock %}
  
{% block body %}
    <div class="container">
        <h2 class="text-center mt-5 mb-3">New Task</h2>
        <div class="card">
            <div class="card-header">
                <a class="btn btn-outline-info float-right" href="{{ path('app_tasks_index') }}"> 
                    View All Tasks
                </a>
            </div>
            <div class="card-body">
                 {{ include('tasks/_form.html.twig') }}
            </div>
        </div>
    </div>
{% endblock %}