{% extends 'base.html.twig' %}
  
{% block title %}Update Task{% endblock %}
  
{% block body %}
    <div class="container">
        <h2 class="text-center mt-5 mb-3">Update Task</h2>
        <div class="card">
            <div class="card-header">
                <a class="btn btn-outline-info float-right" href="{{ path('app_tasks_index') }}"> 
                    View All Tasks
                </a>
            </div>
            <div class="card-body">
                {% include 'tasks/_form.html.twig' with {'button_label': 'Update Task', 'button_color': 'btn-outline-success'}  %}
            </div>
        </div>
    </div>
{% endblock %}